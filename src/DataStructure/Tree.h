#ifndef TREE_H
#define TREE_H
/*
 * Useless class as the nodes are too specific in the case of biomes... Or I should go through a TreeNode class with children, maybe?
#include <vector>

template<class ItemClass>
class Tree
{
public:
    Tree();
    Tree(ItemClass root);

    int getHeight();
    std::vector<ItemClass> getAllItemsAtHeight(int height);

    ItemClass root = nullptr;
};

template<class ItemClass>
        Tree<ItemClass>::Tree()
{

}

template<class ItemClass>
        Tree<ItemClass>::Tree(ItemClass root)
            : root(root)
{

}

template<class ItemClass>
        int Tree<ItemClass>::getHeight()
{

}

template<class ItemClass>
        std::vector<ItemClass> Tree<ItemClass>::getAllItemsAtHeight(int height)
{

}
*/
#endif // TREE_H
